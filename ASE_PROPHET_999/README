Set some variables specific to your filesystem (all within this ASE_PROPHET_999 directory):

For Compile-Machine:
compile_ua.sh: MATLAB_PATH, REPO_DIR, UA_BUILD
For Run-Machine:
mitgcm_run/scripts/compile.sh: MIT_SOURCE
run_coupler.sh: REPO_DIR
run_ua.sh: MCR
config_options.py: expt_name, work_dir, budget_code

################################################
Setup Experiment table:

add expt_name and details about Ua inversion/geometry etc to ../../Ua_InputData/RunTable.csv 

#################################################
Setup MITgcm:

On your local machine:
Make sure to run GenerateInitialDataForMITFile.m from the ua_custom folder to generate the correct initial geometry. The code produces a DataForMIT.mat file required by MITgcm during the spinup phase.
Go to Run-Machine.
python setup_mit_ase_prophet.py
cd mitgcm_run/scripts
./compile.sh
./prepare_run_*.sh where * is the correct experiment (baseline, rcp85, paris2c)
cd ../../

#################################################
Setup Ua:

If not already done, go to Compile-Machine.
./compile_ua.sh
scp Ua <Run-Machine>:<path_to_UaMITgcm>/example/ASE_PROPHET_xxx/ua_run/
copy appropriate restartfile ASE_PROPHET_xxx-RestartFile.mat to ua_custom and ua_run folder on RunMachine

##################################################
Run the model:

sbatch --export=ALL -A <budget code> run_coupler.sh
